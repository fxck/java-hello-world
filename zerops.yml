zerops:
  - setup: app
    build:
      base: java@17
      buildCommands:
        - ./mvnw clean install
      deployFiles:
        - target/*.jar
      cache:
        - ~/.m2

    run:
      base: java@17
      ports:
        - port: 8080
          httpSupport: true
      start: java -jar target/*.jar
